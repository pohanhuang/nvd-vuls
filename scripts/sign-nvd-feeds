#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)

REF_FILE="${REF_FILE:-nvd-feeds.oci-ref}"
LOG_PREFIX="${LOG_PREFIX:-sign-nvd-feeds}"

export REF_FILE LOG_PREFIX

"$SCRIPT_DIR/sign-oci-artifact"
